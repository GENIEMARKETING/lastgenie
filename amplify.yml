version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        - cd client
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: client/.next
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
      - client/node_modules/**/*
